// Parking Lot
    // motorcycle,car,bus 
    // diff amount of space - small, med, large. small =motorcycle. med = car/motor, large = bus.
    //multiple levels
//Methods - park, rremove, total slots, grid system

// class VehicleSize(num{

// }
class ParkingLot {
    constructor(size){
        this.slots = new Array(size).fill(null);
    }
    park(id){
        
    }
}